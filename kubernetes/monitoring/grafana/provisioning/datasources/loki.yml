apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: true

# This configures Grafana to connect to Loki as a datasource.
# Loki provides the log data that Grafana can query and visualize.
